<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>



    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.SelfCareApp"
        tools:targetApi="31">

        <!-- Khai báo SplashActivity là màn hình khởi động -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Khai báo LoginActivity -->
        <activity android:name=".Login.LoginActivity" />

        <!-- Khai báo SignUpActivity -->
        <activity android:name=".Login.SignUpActivity" />
        <activity android:name=".menu.AccountActivity" />
        <activity android:name=".menu.SearchActivity" />
        <activity android:name=".menu.MedicalFragment" />
        <activity android:name=".ChooseHospitalActivity"/>
        <!-- Khai báo MainActivity -->
        <activity android:name=".Login.SplashActivity"
            android:exported="true">

        </activity>
        <activity android:name=".Login.ForgotPassActivity"></activity>

    </application>

</manifest>
